#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main()
{
        ifstream input_file;
        input_file.open("input.txt");

        int total_rolls = 0;
        vector<vector<char>> map;


        cout << "Total rolls of paper: " << total_rolls << endl;
        input_file.close();
        return 0;
}